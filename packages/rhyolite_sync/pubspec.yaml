name: rhyolite_sync
description: Δ-state CRDT file sync engine + wire protocol (client + shared).
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  convergent: any

  rpc_dart: any
  rpc_dart_framework: any
  uuid: ^4.5.3
  rpc_data: any
  rpc_blob: any
  rpc_notify: any
  archive: ^4.0.0
  crypto: ^3.0.7
  path: ^1.9.1
  diff_match_patch: ^0.4.1
  cryptography: ^2.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  fake_async: ^1.3.1
  build_runner: ^2.11.1
  rpc_dart_generator: any
