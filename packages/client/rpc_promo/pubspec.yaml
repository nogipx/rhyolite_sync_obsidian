name: rpc_promo
description: Generic promo code contracts and models (rpc_dart). Pluggable across projects — supply a project-specific binding server-side.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  rpc_dart: any

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  build_runner: ^2.11.1
  rpc_dart_generator: any
